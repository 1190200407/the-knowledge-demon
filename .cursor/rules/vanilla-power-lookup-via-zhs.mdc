---
description: 通过原版 zhs/powers.json 反查能力ID与类名
alwaysApply: true
---

# 原版能力反查流程（优先用中文译名）

当需要“找某个原版能力（Power）的 ID / 类 / 行为”时，按下面步骤：

- **用中文译名定位**：去原版的 `zhs/powers.json`（中文本地化）里搜索该能力的中文译名（例如“吮吸”“硬化外壳”等）。
- **拿到 Power 的 key/ID**：从对应条目的 key（通常形如 `XXX_POWER.title` / `XXX_POWER.description`）得到该 Power 的 **ID**（例如 `SUCK_POWER`）。
- **定位原版类**：在原版源码中搜索该 ID（或 key 字符串），通常能直接定位到 `*Power` 的类定义与触发 hook。

补充：

- 如果中文译名不唯一或搜不到，再回退到英文 `eng/powers.json` 或直接在源码里按效果描述关键词查。
- 反查到的 ID 优先用于：复用原版图标资源（`power_atlas.sprites/<id>.tres` + `images/powers/<id>.png`）以及对照原版行为实现自定义 Power。

# 聚合体特殊事件约定

- **聚合体攻击相关的触发**：不要默认用原版 `AfterAttack(AttackCommand)`，而应优先走本 mod 的 `IAmalgamEventListener`（例如 `OnAmalgamDamagedCreatureAsync`），调用点在 `FriendlyAmalgamHook`。

# 文案对齐原版

- 当新增/改写能力（Power）时，**描述尽量向原版看齐**：优先复用原版的措辞、句式与变量表达（尤其是触发时机、以及按“命中次数/回合次数”结算的表述）。

