---
description: 知识恶魔 mod 卡牌实现约定（动画、打出流程等）
alwaysApply: true
---

# 卡牌实现

## 能力牌打出动画

`CardType.Power` 能力牌在 `OnPlay` 开头须：

```csharp
await CreatureCmd.TriggerAnim(Owner.Creature, "Cast", Owner.Character.CastAnimDelay);
```

技能牌按需使用 `Cast`；攻击牌使用 `Attack` / `heavyAttack` / `superAttack` 等，勿与能力牌混用。
