---
description: 知识恶魔卡池单行卡牌定义格式（无 ID / 怪物 ID）
alwaysApply: true
---

当用户以单行、制表符或空白分隔的格式提供“知识恶魔卡池”卡牌定义时，按以下固定顺序解析字段：

1. 卡牌名称
2. 卡牌类型
3. 卡牌稀有度
4. 卡牌费用
5. 卡牌效果

补充规则：

- 卡牌效果中的括号值表示升级后效果。
- 中文「普通」= `Common`；「罕见」= `Uncommon`；「稀有」= `Rare`。
- 效果中的「（消耗）」表示未升级牌带 `Exhaust` 关键词，升级后去掉消耗；**不要**写进 `cards.json` 描述（系统自动追加）。
- 若卡牌类型或卡牌稀有度为空，视为“未提供”，应向用户确认或按用户当前上下文约定处理。
- 解析后回复时，优先按以上字段名逐项回显，避免错位理解。
