---
description: Do not manually add Godot .uid files for scripts; editor generates them.
alwaysApply: true
---

# Godot `.uid` 文件

- 新增或修改 `Scripts/` 下的 `.cs` 等脚本时，**不要**手动创建、写入或提交对应的 `.uid` 文件。
- 由 Godot 编辑器在需要时自动生成即可。
